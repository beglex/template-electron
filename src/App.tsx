import './App.css';

import {ControlContainer, OutputContainer} from '@root/containers';

export const App = () => {
    return (
        <>
            <ControlContainer />
            <OutputContainer />
        </>
    );
};
